[package]
name = "app"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { workspace = true }
error-stack = { package = "anyhow", workspace = true }
shared = { path = "../../internal/shared" }
